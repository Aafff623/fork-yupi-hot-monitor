# CLAUDE.md

硬约束、验证命令与关键路径以根 `AGENTS.md` 为准，领域事实以 `CONTEXT.md` 为准。
项目：多源热点监控二开（server/ Express 5 + Prisma/SQLite + Socket.IO，client/ React 19 + Vite 7）。
快速验证：`cd server && npm test && npm run build`；`cd client && npm run build`。
维护者 `threetwoa`；不提交密钥与 `.env*`；上游名称仅用于来源与许可证语境。
